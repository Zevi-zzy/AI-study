{
  "name": "zevi-ai-study",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n web,server -c cyan,magenta \"npm run dev --workspace web\" \"npm run dev --workspace server\"",
    "dev:web": "npm run dev --workspace web",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace web && npm run build --workspace server",
    "test": "npm run test --workspace web && npm run test --workspace server",
    "check": "npm run check --workspace web && npm run check --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
